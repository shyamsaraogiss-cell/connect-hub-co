import Image from "next/image";import Link from "next/link";import type { HeroSlideData } from "../types/hero.types";import styles from "../HeroCarousel.module.css";
export function HeroSlide({slide}:{slide:HeroSlideData}){return <article className={styles.slide} aria-labelledby={`hero-title-${slide.id}`}><Image className={styles.templeBackground} src="/images/hero/Hero_1_Gaya_Temple_Background_v1.0.png" alt="" fill priority sizes="(max-width: 680px) 100vw, (max-width: 1100px) 65vw, 60vw"/><div className={styles.ivoryOverlay} aria-hidden="true"/><div className={styles.heroCopy}><Image className={styles.heroLogo} src={slide.logoSrc} alt={slide.logoAlt} width={150} height={150} priority/><h1 id={`hero-title-${slide.id}`}>{slide.title}</h1><p className={styles.tagline}>{slide.tagline}</p><div className={styles.ornament} aria-hidden="true"><span/>◆<span/></div><p className={styles.supportingCopy}>{slide.supportingCopy.map(line=><span key={line}>{line}</span>)}</p><ul className={styles.servicePoints} aria-label="Available service points">{slide.servicePoints.map(point=><li key={point.label}><span aria-hidden="true">✓</span>{point.label}</li>)}</ul></div><p className={styles.brandProof}>The Authentic Ancestral Rites | Verified Lineage | Vedic Precision</p><Link className={styles.locationCard} href={slide.primaryHref} aria-label={`${slide.location}, ${slide.locationSubtitle}. Learn more`}><span className={styles.pin} aria-hidden="true">●</span><span><strong>{slide.location}</strong><small>{slide.locationSubtitle}</small></span></Link></article>}
