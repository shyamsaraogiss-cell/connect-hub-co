"use client";
import Link from "next/link";import { useEffect,useState } from "react";import { getPublicServices } from "@/services/service-catalog.api";import type { CatalogService } from "@/types/service-catalog";
export function FeaturedServices(){const [items,setItems]=useState<CatalogService[]>([]);const [loading,setLoading]=useState(true);useEffect(()=>{void getPublicServices({featured:true,page:1,pageSize:3}).then(r=>setItems(r.items)).finally(()=>setLoading(false))},[]);return <section className="bg-[#fffaf0] px-6 py-16"><div className="mx-auto max-w-7xl"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="font-semibold uppercase tracking-widest text-orange-800">Services</p><h2 className="mt-2 text-3xl font-bold">Featured ritual support</h2></div><Link className="font-semibold text-orange-800" href="/services">View all services →</Link></div>{loading?<p className="mt-8" role="status">Loading services…</p>:items.length?<div className="mt-8 grid gap-6 md:grid-cols-3">{items.map(item=><Link href={`/services/${item.slug}`} className="rounded-2xl border border-orange-100 bg-white p-7 shadow-sm hover:shadow-md" key={item.id}><p className="text-xs font-bold uppercase tracking-widest text-orange-700">{item.categoryName}</p><h3 className="mt-3 text-xl font-bold">{item.name}</h3><p className="mt-3 text-stone-600">{item.shortDescription||item.description}</p><span className="mt-5 inline-block font-semibold text-orange-800">View service →</span></Link>)}</div>:<div className="mt-8 rounded-2xl border border-dashed border-orange-300 bg-white p-8 text-center"><p>Our ritual catalog is being prepared for public viewing.</p><Link className="mt-3 inline-block font-semibold text-orange-800" href="/pitru-moksha">Plan Pitru Moksha Gaya</Link></div>}</div></section>}
