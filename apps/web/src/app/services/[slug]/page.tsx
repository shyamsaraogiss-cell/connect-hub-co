import type { Metadata } from "next";import { PublicServiceDetail } from "@/components/service-catalog/PublicServiceDetail";export const metadata:Metadata={title:"Service details | Connect Hub Co",description:"Service information, eligibility, materials, duration, and enquiry options."};export default async function ServicePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;return <PublicServiceDetail slug={slug}/>}
