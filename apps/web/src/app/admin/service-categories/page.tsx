"use client";import Link from "next/link";import { FormEvent,useEffect,useState } from "react";import { createCategory,deleteCategory,getAdminCategories,updateCategory } from "@/services/service-catalog.api";import { CategoryInput,ServiceCategory } from "@/types/service-catalog";
const blank:CategoryInput={name:"",slug:"",description:"",active:true,sortOrder:0,defaultLocale:"en"};
export default function CategoriesPage(){const [items,setItems]=useState<ServiceCategory[]>([]);const [form,setForm]=useState(blank);const [editing,setEditing]=useState<string|null>(null);const [error,setError]=useState<string|null>(null);async function load(){setItems(await getAdminCategories());}useEffect(()=>{let cancelled=false;void getAdminCategories().then(rows=>{if(!cancelled)setItems(rows);}).catch(reason=>{if(!cancelled)setError(reason instanceof Error?reason.message:"Unable to load categories.");});return()=>{cancelled=true;};},[]);function edit(item:ServiceCategory){setEditing(item.id);setForm({name:item.name,slug:item.slug,description:item.description??"",active:item.active,sortOrder:item.sortOrder,defaultLocale:item.defaultLocale});}async function submit(event:FormEvent){event.preventDefault();setError(null);try{if(editing)await updateCategory(editing,form);else await createCategory(form);setEditing(null);setForm(blank);await load();}catch(reason){setError(reason instanceof Error?reason.message:"Unable to save category.");}}async function remove(id:string){if(!confirm("Delete this empty category?"))return;try{await deleteCategory(id);await load();}catch(reason){setError(reason instanceof Error?reason.message:"Unable to delete category.");}}return <main className="min-h-screen bg-gray-100 p-4 sm:p-8"><div className="mx-auto max-w-6xl"><div className="flex items-center justify-between"><h1 className="text-3xl font-bold">Service Categories</h1><Link className="text-blue-700" href="/admin/services">Manage services</Link></div>{error?<p className="mt-4 rounded bg-red-50 p-3 text-red-700">{error}</p>:null}<form onSubmit={submit} className="mt-6 grid gap-3 rounded-xl bg-white p-5 shadow sm:grid-cols-3"><input className="rounded border p-2" placeholder="Category name" value={form.name} onChange={e=>setForm({...form,name:e.target.value})} required/><input className="rounded border p-2" placeholder="seo-slug" value={form.slug} onChange={e=>setForm({...form,slug:e.target.value})} required/><input className="rounded border p-2" type="number" min={0} placeholder="Sort order" value={form.sortOrder} onChange={e=>setForm({...form,sortOrder:Number(e.target.value)})}/><textarea className="rounded border p-2 sm:col-span-2" placeholder="Description" value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/><div className="flex items-center gap-4"><label><input type="checkbox" checked={form.active} onChange={e=>setForm({...form,active:e.target.checked})}/> Active</label><button className="rounded bg-blue-700 px-4 py-2 text-white">{editing?"Update":"Create"}</button></div></form><div className="mt-6 overflow-x-auto rounded-xl bg-white shadow"><table className="w-full text-left"><thead><tr className="border-b"><th className="p-3">Name</th><th>Slug</th><th>Services</th><th>Status</th><th>Actions</th></tr></thead><tbody>{items.map(item=><tr className="border-b" key={item.id}><td className="p-3 font-medium">{item.name}</td><td>{item.slug}</td><td>{item.serviceCount}</td><td>{item.active?"Active":"Inactive"}</td><td><button className="mr-3 text-blue-700" onClick={()=>edit(item)}>Edit</button><button className="text-red-700" onClick={()=>void remove(item.id)}>Delete</button></td></tr>)}</tbody></table></div></div></main>}
