import type { Metadata } from "next";
export const metadata: Metadata = { title: "Zen G Ritual AI", description: "Responsible AI-assisted navigation and informational guidance for Connect Hub Co ritual and pilgrimage services." };
export default function ZenGLayout({ children }: { children: React.ReactNode }) { return children; }
