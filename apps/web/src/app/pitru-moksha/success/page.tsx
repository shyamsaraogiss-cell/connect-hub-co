import type { Metadata } from "next";
import Link from "next/link";
export const metadata: Metadata = { title: "Request received | PitruMoksha Gaya", robots: { index: false, follow: false } };
export default async function SuccessPage({ searchParams }: { searchParams: Promise<{ request?: string }> }) { const { request } = await searchParams; return <main className="grid min-h-screen place-items-center bg-amber-50 p-6"><div className="max-w-lg rounded-3xl bg-white p-10 text-center shadow-xl"><div className="text-5xl">✓</div><h1 className="mt-5 text-3xl font-bold">Your request is received</h1><p className="mt-4 text-stone-600">A Connect Hub Co coordinator will review the details and contact you to confirm the ritual plan and religious partner.</p>{request ? <Link className="mt-7 block rounded-xl bg-orange-700 px-5 py-3 font-semibold text-white" href={`/travel-assistance?request=${encodeURIComponent(request)}`}>Add travel assistance</Link> : null}<Link className="mt-5 inline-block text-orange-800 underline" href="/pitru-moksha">Return to PitruMoksha Gaya</Link></div></main>; }
